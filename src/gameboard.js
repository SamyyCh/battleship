// import Ship from './ship';

class Gameboard {
    constructor() {
        this.height = 10,
        this.width = this.height,
        this.size = this.height * this.width,
        this.map = []
    }

    placeShip() {
        const xCord = [];
        const yCord = [];
        return this.position = [xCord, yCord]
    }

    receiveAttack(xCord, yCord) {
        
    }
}

module.exports = Gameboard;